#
	
if NUC505
source "$KCONFIG_START_DIR_PATH/hw_drivers/I2S/I2S_nuc505/Kconfig"
endif

config INTERNAL_I2S_SPEED_CRITICAL
	bool   "internal i2s is speed critical"
	depends on INCLUDE_INTERNAL_I2S
	default y
